import React from 'react';

import classNames from 'classnames/bind';
import styles from './Order.module.scss';
const cx = classNames.bind(styles);
const Order = () => {
    return <div className={cx('Order')}>oder</div>;
};

export default Order;
